import { DirtyErrorStateMatcher } from './dirty-error-state-matcher';

describe('DirtyErrorStateMatcher', () => {
  it('should create an instance', () => {
    expect(new DirtyErrorStateMatcher()).toBeTruthy();
  });
});
